import React from 'react';
import ReactDOM from 'react-dom/client';
import FoodMenuGenerator from './components/FoodMenuGenerator';

// Create a root and render the component
const root = ReactDOM.createRoot(document.getElementById('foodMenuApp'));
root.render(<FoodMenuGenerator />);